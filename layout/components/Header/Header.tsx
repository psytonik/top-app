import React, {useEffect, useState} from 'react';import {HeaderProps} from "./Header.props";import cn from "classnames";import styles from './Header.module.css';import Logo from "../../../public/logo.svg";import ButtonIcon from "../../../components/ButtonIcon/ButtonIcon";import { motion } from 'framer-motion';import Sidebar from "../Sidebar/Sidebar";import {useRouter} from "next/router";const Header = ({className, ...props}:HeaderProps):JSX.Element => {    const [isOpen,setIsOpen] = useState<boolean>(false);    const router = useRouter();    useEffect(() =>{        setIsOpen(false);    },[router]);    const variants = {        opened:{            opacity:1,            x:0,            transition:{                stiffness: 20,            }        },        closed:{            opacity:0,            x:'100%'        }    };    return (        <header className={cn(className, styles.header)} {...props}>            <Logo/>            <ButtonIcon icon={'hamburger'} appearance={'white'} onClick={()=>setIsOpen(true)}/>            {isOpen && (<motion.div className={styles.mobileMenu} variants={variants} initial={'closed'} animate={isOpen ? 'opened' : 'closed'}>                <Sidebar />                <ButtonIcon className={styles.menuCloseIcon} icon={'close'} appearance={'white'} onClick={()=>setIsOpen(false)}/>            </motion.div>)}        </header>    );};export default Header;