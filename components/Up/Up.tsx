import React, {useEffect} from 'react';import styles from './Up.module.css';import {motion, useAnimation} from "framer-motion";import {useScrollY} from "../../hooks/useScrollY";import ButtonIcon from "../ButtonIcon/ButtonIcon";const Up = ():JSX.Element => {    const controls = useAnimation();    const y = useScrollY();    const scrollToTop = () => {        window.scrollTo({            top: 0,            behavior:'smooth'        });    };    useEffect(() =>{        controls.start({opacity: y / document.body.scrollHeight}).then(r => r);    },[y,controls]);    return (        <motion.div            className={styles.up}            animate={controls}            initial={{opacity:0}}        >            <ButtonIcon                appearance={'white'}                icon={'up'}                onClick={scrollToTop}                aria-label="scrollToTop"            />        </motion.div>    );};export default Up;