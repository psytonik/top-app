import React from 'react';import {ReviewProps} from "./Review.props";import styles from './Review.module.css';import cn from "classnames";import UserIcon from './userIcon.svg';import Rating from "../Rating/Rating";import {format} from 'date-fns';const Review = ({review, className,...props}:ReviewProps):JSX.Element => {    const {name,rating,title,description,createdAt} = review;    return (        <div className={cn(styles.review, className)} {...props}>            <UserIcon className={styles.userLogo}/>            <div className={styles.reviewerTitle}>                <span className={styles.reviewerName}>{name}:</span>&nbsp;&nbsp;                <span>{title}:</span>            </div>                <div className={styles.reviewerCreatedAt}>                    {format(new Date(createdAt),'dd-MMMM-yyyy')}                </div>                <div className={styles.reviewerRating}>                    <Rating  rating={rating}/>                </div>                <div className={styles.reviewerDescription}>                    {description}                </div>        </div>    );};export default Review;